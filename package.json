{
  "name": "dzfu",
  "private": true,
  "devDependencies": {
    "lerna": "^3.15.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "build": "lerna run build",
    "lerna:publish": "lerna publish from-git"
  }
}
